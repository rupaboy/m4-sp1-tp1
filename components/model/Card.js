class Card {
    constructor(title, description, imgLink, imgTitle, imgDescription) {
    this.title = title;
    this.description = description;
    this.imgLink = imgLink;
    this.imgTitle = imgTitle;
    this.imgDescription = imgDescription;
    }
}

const Card = new Card()